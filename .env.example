# Used by docker-compose.yml to set the backend's JWT signing key.
# Generate a long random value for any real deployment, e.g.: openssl rand -base64 48
JWT_KEY=CHANGE_ME_TO_A_LONG_RANDOM_SECRET_AT_LEAST_32_CHARS
